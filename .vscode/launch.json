{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Analyse",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "args": [
                "${command:pickArgs}"
            ],
            "cwd": "${workspaceFolder}/data/Archive/labelled/analyse_agreement"
        }
    ]
}
